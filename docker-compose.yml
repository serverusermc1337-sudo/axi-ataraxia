services:
  axi:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - axi-data:/data
    environment:
      DATA_DIR: /data
      WEB_PORT: "8787"
    ports:
      - "8787:8787"
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  axi-data:
